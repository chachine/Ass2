
<%= simple_form_for(@att_atock) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :type %>
    <%= f.input :numero %>
    <%= f.input :used %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
